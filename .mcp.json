{
  "mcpServers": {
    "datadog": {
      "type": "remote",
      "url": "https://mcp.datadoghq.com/sse",
      "headers": {
        "DD-API-KEY": "${DATADOG_API_KEY}",
        "DD-APPLICATION-KEY": "${DATADOG_APP_KEY}"
      }
    }
  }
}
